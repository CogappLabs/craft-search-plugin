{% import "_includes/forms" as forms %}

{{ forms.selectField({
    label: "Search Index"|t('search-index'),
    instructions: "The search index to search within."|t('search-index'),
    id: 'indexHandle',
    name: 'indexHandle',
    value: field.indexHandle,
    options: indexOptions,
    required: true,
}) }}

{{ forms.textField({
    label: "Results Per Page"|t('search-index'),
    instructions: "Number of search results shown per request in the field input."|t('search-index'),
    id: 'perPage',
    name: 'perPage',
    value: field.perPage,
    type: 'number',
    min: 1,
    max: 100,
    size: 5,
}) }}
